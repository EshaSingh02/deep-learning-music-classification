# Dataset

The dataset is not included in this repository.

The supplied notebook uses the course/competition dataset `cs-776-competition-2025-2026-sem-2` and reads training metadata from `dataset_music/train/metadata.csv`.

Obtain the dataset through the original course/competition environment and pass its local root directory to the training/evaluation scripts.

Do not commit the dataset or any private credentials to GitHub.
